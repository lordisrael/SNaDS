
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - NODE_ENV=production
    container_name: snads-backend
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env
    environment:
      - NODE_ENV=${NODE_ENV_PROD}
      - MONGO_URI=${MONGO_URI_PROD}
    command: npm start
    depends_on: []

   